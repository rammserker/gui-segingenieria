# Tests de capacidades del generador

## Bloque ELSE

Aquí tengo un párrafo que si la variable test.bloqueif es TRUE, entonces {{ @if test.bloqueif }}dirá que efectivamente es TRUE{{ @else }}diría que es TRUE, pero no lo es{{ @end }}.
